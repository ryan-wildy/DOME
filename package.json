{
  "name": "dome-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Dome by PrintoDome launch MVP: public site, registrations, OTP, admin approval and lead workflows.",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
